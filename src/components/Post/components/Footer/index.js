import React from 'react';
import {Text, View} from "react-native";

const Footer = () => {
    return (
        <View>
            <Text>Footer</Text>
        </View>
    );
};

export default Footer;
