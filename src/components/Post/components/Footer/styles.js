import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    likes: {
        fontWeight: "bold",
    },
    caption: {

    },
    postedAt: {
        color: "#8c8c8c",
    }
});

export default styles;
